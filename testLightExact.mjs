import { fetchGoogleSheetMenu } from './src/lib/sheetFetcher.ts';

// Because we need Vite env vars, we might need a little ts-node setup.
// Let's just create a custom fetcher using fs instead of internet, or since it's just a test, I can read the csv and run the exact same function text.

